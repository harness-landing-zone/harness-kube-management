# Test spoke — triggers harness-eso-secrets AppSet to test valuesObject bug
clusterName: test-spoke
namespace: harness-agent
destinationCluster: hub

secretStore:
  enabled: false
  kind: ClusterSecretStore
  name: fake-backend

apiKey:
  targetSecretName: harness-api-key-secret
  secretManagerKey: "harness/api-key"

clusterRegistration:
  enabled: false
